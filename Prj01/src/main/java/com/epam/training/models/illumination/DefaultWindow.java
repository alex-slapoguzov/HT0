package com.epam.training.models.illumination;

public class DefaultWindow extends Window {

    private static final int DEFAULT_WINDOW_SIZE = 700;

    public DefaultWindow() {
        super(DEFAULT_WINDOW_SIZE);
    }
}
